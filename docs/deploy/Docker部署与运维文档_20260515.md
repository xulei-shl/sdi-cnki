# M10: Docker 部署编排与运维文档 设计文档

- **Status**: Proposal
- **Date**: 2026-05-15

## 1. 目标与背景

实现生产级 Docker 部署方案，包含：
1. 后端 FastAPI 多阶段 Dockerfile（含 Worker 进程）
2. `docker-compose.yml` 编排所有服务（API、Worker、Nginx）
3. 部署运维手册与用户操作手册
4. 验收测试报告

---

## 2. 详细设计

### 2.1 项目结构变更

```
sdi-cnki/
├── backend/
│   ├── Dockerfile              # [新增] 后端多阶段构建
│   ├── .dockerignore           # [新增]
│   └── docker-entrypoint.sh    # [新增] 容器入口脚本
├── docker-compose.yml          # [新增] 服务编排
├── docker/
│   ├── nginx/
│   │   └── default.conf        # [新增] Nginx 反向代理配置
│   └── entrypoint.sh           # [新增] 综合入口（可选）
└── docs/
    └── deploy/
        ├── 部署运维手册.md       # [新增]
        └── 用户操作手册.md       # [新增]
```

### 2.2 后端 Dockerfile

**位置**: `backend/Dockerfile`

```dockerfile
# === Build Stage ===
FROM python:3.11-slim as builder

WORKDIR /build
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -t /deps

# === Runtime Stage ===
FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl ca-certificates && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY --from=builder /deps /usr/local/lib/python3.11/site-packages
COPY . .

# 创建数据目录
RUN mkdir -p /app/data/uploads /app/data/downloads /app/data/exports /app/data/cookies

# 暴露端口
EXPOSE 8000

# 入口脚本处理 Worker/API 启动
COPY docker-entrypoint.sh /docker-entrypoint.sh
RUN chmod +x /docker-entrypoint.sh
ENTRYPOINT ["/docker-entrypoint.sh"]
```

**入口脚本**: `backend/docker-entrypoint.sh`

```bash
#!/bin/bash
set -e

case "$1" in
  api)
    exec uvicorn app.main:app --host 0.0.0.0 --port 8000
    ;;
  worker)
    QUEUE_TYPE="${2:-cnki}"
    CONCURRENCY="${3:-1}"
    exec python worker_runner.py "$QUEUE_TYPE" "$CONCURRENCY"
    ;;
  *)
    exec "$@"
    ;;
esac
```

### 2.3 Docker Compose

**位置**: `docker-compose.yml`

```yaml
version: "3.8"

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: cnki-service-api:latest
    container_name: cnki-api
    command: ["api"]
    ports:
      - "8000:8000"
    volumes:
      - cnki_data:/app/data
      - ./backend/.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  worker-cnki:
    image: cnki-service-api:latest
    container_name: cnki-worker-cnki
    command: ["worker", "cnki", "1"]
    volumes:
      - cnki_data:/app/data
      - ./backend/.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy

  worker-llm:
    image: cnki-service-api:latest
    container_name: cnki-worker-llm
    command: ["worker", "llm", "5"]
    volumes:
      - cnki_data:/app/data
      - ./backend/.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy

  worker-download:
    image: cnki-service-api:latest
    container_name: cnki-worker-download
    command: ["worker", "download", "1"]
    volumes:
      - cnki_data:/app/data
      - ./backend/.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy

  worker-export:
    image: cnki-service-api:latest
    container_name: cnki-worker-export
    command: ["worker", "export", "2"]
    volumes:
      - cnki_data:/app/data
      - ./backend/.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy

  nginx:
    image: nginx:stable-alpine
    container_name: cnki-nginx
    ports:
      - "8848:8848"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./dist:/usr/share/nginx/html:ro
    depends_on:
      - api
    restart: unless-stopped

volumes:
  cnki_data:
    driver: local
```

### 2.4 Nginx 配置

**位置**: `docker/nginx/default.conf`

```nginx
upstream backend {
    server api:8000;
}

server {
    listen 8848;
    server_name localhost;

    # 前端静态文件
    root /usr/share/nginx/html;
    index index.html;

    # 静态资源缓存
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 必需配置
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 2.5 多环境配置

| 环境 | 配置文件 | 用途 |
|------|----------|------|
| 开发 | `.env.development` | 本地开发，默认值 |
| 预发布 | `.env.staging` | CI/CD 预发布验证 |
| 生产 | `.env.production` | 生产部署 |

**关键配置项**：

```env
# 必须修改
JWT_SECRET_KEY=<生产随机密钥>
AES_ENCRYPTION_KEY=<生产随机32位密钥>
CNKI_USERNAME=<知网账号>
CNKI_PASSWORD=<知网密码>

# 按需修改
DATABASE_URL=sqlite+aiosqlite:///./data/cnki_service.db
WEBHOOK_ENTERPRISE_WECHAT=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx
```

### 2.6 部署步骤

```bash
# 1. 构建前端
pnpm build

# 2. 复制前端产物
cp -r dist/ docker/nginx/html/

# 3. 配置环境变量
cp .env.production backend/.env
# 编辑 backend/.env 填充敏感信息

# 4. 构建并启动
docker-compose up -d --build

# 5. 初始化数据库
docker exec cnki-api python -c "
import asyncio
from app.database import init_db, engine
from app.dependencies import hash_password
from sqlalchemy import text
async def s():
    await init_db()
    async with engine.begin() as c:
        await c.execute(text(\"INSERT OR IGNORE INTO users (username, password_hash, email, role, is_active) VALUES ('admin', :pw, 'admin@example.com', 'admin', 1)\"), {'pw': hash_password('admin123')})
    print('DB initialized')
    await engine.dispose()
asyncio.run(s())
"

# 6. 验证
curl http://localhost:8848/api/v1/health
# {"status": "ok"}
```

### 2.7 运维监控

| 操作 | 命令 |
|------|------|
| 查看日志 | `docker-compose logs -f api` |
| 重启服务 | `docker-compose restart api` |
| 查看磁盘 | `docker system df` |
| 数据备份 | `docker cp cnki-api:/app/data ./backup/` |
| 数据恢复 | `docker cp ./backup/data cnki-api:/app/` |

### 2.8 验收测试清单

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | `docker-compose up -d --build` | 所有容器正常启动，无错误日志 |
| 2 | `curl localhost:8848` | 返回前端 HTML |
| 3 | `curl localhost:8848/api/v1/health` | `{"status": "ok"}` |
| 4 | 前端登录页加载 | 输入 admin/admin123 可登录 |
| 5 | 创建任务模板并执行 | 端到端流程正常 |
| 6 | Worker 进程正常消费 | 各队列任务可正常处理 |
| 7 | `docker-compose restart api` | 服务恢复，数据不丢失 |
| 8 | `docker-compose down && up` | 数据持久化正常 |
| 9 | 磁盘空间不足告警 | 日志打印告警信息 |

---

## 3. 文件清单

| 文件 | 说明 | 状态 |
|------|------|------|
| `backend/Dockerfile` | 后端多阶段构建 | 新增 |
| `backend/.dockerignore` | Docker 构建忽略 | 新增 |
| `backend/docker-entrypoint.sh` | 容器入口脚本 | 新增 |
| `docker-compose.yml` | 服务编排 | 新增 |
| `docker/nginx/default.conf` | Nginx 反向代理配置 | 新增 |
| `docs/deploy/部署运维手册.md` | 运维指南 | 新增 |
| `docs/deploy/用户操作手册.md` | 用户指南 | 新增 |
| `.env.staging` | 预发布环境配置 | 已存在 |
| `.env.production` | 生产环境配置 | 已存在 |
